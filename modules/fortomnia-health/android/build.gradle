apply plugin: 'com.android.library'
apply plugin: 'expo-module-gradle-plugin'

group = 'expo.modules.fortomniahealth'
version = '1.0.0'

android {
  namespace 'expo.modules.fortomniahealth'
  defaultConfig {
    versionName '1.0.0'
  }
}

dependencies {
  implementation 'androidx.health.connect:connect-client:1.1.0'
}
